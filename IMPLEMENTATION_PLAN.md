# IMPLEMENTATION_PLAN.md

> Generated by `./loop.sh plan` -- do not edit manually until planning is complete.
> The Plan Architect agent replaces this file with a full task list after analyzing your specs.

## Summary

**Status**: Not yet planned -- run `./loop.sh plan` to generate.

**Scope**: [Number of specs, what they cover]

**Stack**: [From CLAUDE.md -- framework, ORM, DB, auth, package manager, test runner]

**Key architectural decisions**:
- [Decision 1 -- e.g., derived types from ORM schema, no manual interfaces]
- [Decision 2 -- e.g., service layer factory pattern]
- [Decision 3 -- e.g., route registration pattern]

**Codebase root**: [Absolute path]

---

<!-- ═══════════════════════════════════════════════════════════════════════
     TASK FORMAT REFERENCE — Delete this section after planning generates real tasks.

     Every task MUST include ALL of these fields:
       - Title with task number
       - Priority (integer, lower = higher priority)
       - Spec (which spec file this implements)
       - Complexity (S / M / L)
       - Acceptance criteria (one sentence)
       - Files to create / Files to modify (with exact paths and what to add)
       - Required Tests (categorized by type, with [ ] checkboxes)
       - Dependencies (task numbers this depends on)
       - Blocks (task numbers this unblocks)

     Below are EXAMPLE tasks showing each task type. These are TEMPLATES
     for the Plan Architect to follow — replace with real tasks.
     ═══════════════════════════════════════════════════════════════════════ -->

---

## Task Type Examples (delete after planning)

### EXAMPLE — Schema / Data Model Task

```
### Task N: [Entity] schema table definition
**Priority**: N
**Spec**: `specs/NNN-schema.md`
**Complexity**: S
**Acceptance criteria**: [table] with correct types, constraints, UNIQUE([columns]), INDEX([columns]), FK to [parent], default values

**Files to create**:
- `[path/to/schema/entity.ts]`

**Files to modify**:
- `[path/to/schema/index.ts]` -- add `export { entity } from "./entity.js";`

**Required Tests**:
Unit:
- [ ] Schema exports `entity` table object
- [ ] `$inferSelect` derives all expected columns ([list every column])
- [ ] `$inferInsert` makes optional fields optional and required fields required
- [ ] Schema barrel export includes `entity`

Source verification:
- [ ] Defines UNIQUE constraint on ([columns])
- [ ] Defines INDEX on ([columns])
- [ ] Has FK reference to `[parent].id` [with onDelete behavior]
- [ ] [Column] has default `[value]`

**Dependencies**: [Task numbers or None]
**Blocks**: [Task numbers]
```

### EXAMPLE — Migration Task

```
### Task N: Generate migration for all [domain] tables
**Priority**: N
**Spec**: `specs/NNN-schema.md`
**Complexity**: S
**Acceptance criteria**: Migration generated via [tool], reviewed for correct FK cascade behavior, applies cleanly

**Files created (auto-generated)**:
- `[path/to/migrations/XXXX_description.sql]`

**Required Tests**:
Source verification:
- [ ] Migration SQL file exists and is non-empty
- [ ] Migration contains CREATE TABLE statements for all N tables: [list]
- [ ] Migration contains expected UNIQUE constraints
- [ ] Migration contains ON DELETE CASCADE for child table FKs

**Dependencies**: [All schema tasks]
**Blocks**: [Seed task, service tasks]
```

### EXAMPLE — Seed Data Task

```
### Task N: Seed data -- [entities] with full [relationship trees]
**Priority**: N
**Spec**: `specs/NNN-seed-data.md`
**Complexity**: M
**Acceptance criteria**: [count] [entities] seeded idempotently; [specific data requirements]; mixed statuses

**Files to create**:
- `[path/to/seed/entity.ts]`

**Files to modify**:
- `[path/to/seed.ts]` -- import and call seed function after existing seeding

**Required Tests**:
Unit:
- [ ] Seed script exports a callable function (not just top-level side effects)
- [ ] Seed data defines [count] [entities] across all [categories]
- [ ] Each [entity] has realistic [domain-specific] data
- [ ] [Relationship-specific assertions]

Integration:
- [ ] Seed is idempotent -- running twice does not create duplicates
- [ ] All FK relationships are valid after seed (no orphaned references)

**Dependencies**: [Migration task]
**Blocks**: [UI tasks that need data to render]
```

### EXAMPLE — Validator / Type Task

```
### Task N: [Entity] Zod validators and derived types
**Priority**: N
**Spec**: `specs/NNN-crud.md`
**Complexity**: S
**Acceptance criteria**: create/update/bulk schemas exported; derived types exported; enums match spec exactly

**Files to create**:
- `[path/to/validators/entity.ts]`
- `[path/to/types/entity.ts]`

**Files to modify**:
- `[path/to/validators/index.ts]` -- add exports
- `[path/to/types/index.ts]` -- add type exports

**Required Tests**:
Unit:
- [ ] `createSchema` accepts valid input with all fields
- [ ] `createSchema` accepts minimal input (required fields only, uses defaults)
- [ ] `createSchema` rejects empty [required field] (min 1 char)
- [ ] `createSchema` rejects [field] longer than [max] chars
- [ ] `createSchema` rejects invalid enum value (e.g., "[invalid]")
- [ ] `createSchema` rejects negative [numeric field]
- [ ] `updateSchema` is partial -- all fields optional
- [ ] `bulkSchema` requires min 1 item, max [limit]
- [ ] `bulkSchema` rejects empty array
- [ ] [Enum] includes all [N] values: [list]

**Dependencies**: None (validators are framework-independent)
**Blocks**: [Service tasks, route tasks]
```

### EXAMPLE — Pure Function Task

```
### Task N: [Computation name] pure function
**Priority**: N
**Spec**: `specs/NNN.md`
**Complexity**: S
**Acceptance criteria**: `functionName(inputs)` pure function returns [output shape] or null; extracted to lib/

**Files to create**:
- `[path/to/lib/function-name.ts]`

**Required Tests**:
Unit:
- [ ] Computes correct result from valid inputs
- [ ] Returns null if [missing data condition]
- [ ] Handles [edge case 1] correctly
- [ ] Handles [edge case 2] correctly
- [ ] Returns [zero/empty state] for [boundary input]
- [ ] Does not divide by zero (guards per CLAUDE.md unsafe math rule)
- [ ] Pure function -- no side effects, no DB calls, no imports from services

**Dependencies**: [Type definition tasks]
**Blocks**: [Service tasks that use this function]
```

### EXAMPLE — Service Layer Task

```
### Task N: [Entity] service layer
**Priority**: N
**Spec**: `specs/NNN-crud.md`
**Complexity**: M
**Acceptance criteria**: All [N] service methods ([list]) implemented following [pattern] factory pattern

**Files to create**:
- `[path/to/services/entity.ts]`

**Files to modify**:
- `[path/to/services/index.ts]` -- add export

**Required Tests**:
Unit/Integration:
- [ ] `list(scopeId)` returns all [entities] for the scope ordered by [field] ASC
- [ ] `list(scopeId, { search: "[term]" })` filters by [field] ILIKE with proper escaping
- [ ] `list` ILIKE escapes backslash first, then %, then _ (per CLAUDE.md SQL LIKE escaping rule)
- [ ] `list(scopeId, { [filter]: "[value]" })` filters by exact match
- [ ] `getById(id)` returns [entity] or null (not empty object)
- [ ] `create(scopeId, data)` inserts and returns the new [entity]
- [ ] `create` fails with specific error on duplicate [unique constraint]
- [ ] `update(id, data)` sets updatedAt to new Date()
- [ ] `update` with undefined [field] does NOT update that field (guard with !== undefined)
- [ ] `remove(id)` checks for referencing [children]; returns error with count if references exist
- [ ] `remove(id)` returns null for non-existent id

**Dependencies**: [Schema, migration, validator tasks]
**Blocks**: [Route tasks]
```

### EXAMPLE — API Routes Task

```
### Task N: [Entity] routes layer
**Priority**: N
**Spec**: `specs/NNN-crud.md`
**Complexity**: M
**Acceptance criteria**: All [N] route endpoints registered following [pattern] with auth, validation, activity logging

**Files to create**:
- `[path/to/routes/entity.ts]`

**Files to modify**:
- `[path/to/routes/index.ts]` -- add export
- `[path/to/app.ts]` -- mount router

**Required Tests**:
Integration (supertest or equivalent):
- [ ] `GET /[path]` returns 200 with array
- [ ] `GET /[path]?[filter]=[value]` filters correctly
- [ ] `GET /[path]/:id` returns 200 with [entity], or 404 with `{ error: "[Entity] not found" }`
- [ ] `POST /[path]` creates [entity], returns 201
- [ ] `POST /[path]` returns 409 on duplicate [unique field]
- [ ] `PATCH /[path]/:id` updates [entity], returns 200
- [ ] `PATCH /[path]/:id` returns 404 for non-existent id
- [ ] `DELETE /[path]/:id` deletes [entity], returns 200
- [ ] `DELETE /[path]/:id` returns 409 with [child] count when [entity] is in use
- [ ] All routes enforce auth/access control
- [ ] POST routes use `validate([createSchema])` middleware
- [ ] PATCH routes use `validate([updateSchema])` middleware
- [ ] Mutation routes log activity: [entity].created, [entity].updated, [entity].deleted

**Dependencies**: [Validator, service tasks]
**Blocks**: [UI API client tasks]
```

### EXAMPLE — AI / External Service Task

```
### Task N: [AI feature] service
**Priority**: N
**Spec**: `specs/NNN-ai.md`
**Complexity**: L
**Acceptance criteria**: [Method] calls [AI provider] with structured output; try-catch on all external calls; retry with backoff; generation ID tracking

**Files to create**:
- `[path/to/services/ai-feature.ts]`

**Required Tests**:
Unit (with mocked AI client -- external service calls must have try-catch per CLAUDE.md):
- [ ] Calls [AI SDK method] with correct schema
- [ ] Uses prompt template from [prompt file] (not inline string)
- [ ] Validates AI response against schema
- [ ] Returns `{ error: "[failure message]", details: ... }` when AI call throws
- [ ] Returns `{ error: "[validation message]", details: zodError }` on validation failure
- [ ] Retries with exponential backoff on transient errors (max 3 retries)
- [ ] Generates unique ID per call and includes it in response
- [ ] [Batch method] processes N items with concurrency limit (default [N])
- [ ] [Batch method] returns partial results + per-item errors (not all-or-nothing)

Integration:
- [ ] Full pipeline: [step 1] -> [step 2] -> result is a valid [output type]

**Dependencies**: [Prompt template task, resolver task]
**Blocks**: [API endpoint tasks]
```

### EXAMPLE — UI API Client + Hooks Task

```
### Task N: [Entity] API client and React Query hooks
**Priority**: N
**Spec**: `specs/NNN-ui.md`
**Complexity**: S
**Acceptance criteria**: API client wraps all [entity] endpoints; query hooks for list/detail; mutation hooks with cache invalidation

**Files to create**:
- `[path/to/api/entity.ts]`
- `[path/to/hooks/useEntity.ts]`

**Files to modify**:
- `[path/to/api/index.ts]` -- add exports

**Required Tests**:
Unit:
- [ ] API client exports functions matching all route endpoints
- [ ] Each function calls correct HTTP method and path
- [ ] List function passes filter params as query string
- [ ] Mutation functions send body as JSON
- [ ] React Query hooks export: useEntityList, useEntityDetail, useCreateEntity, useUpdateEntity, useDeleteEntity
- [ ] Mutations invalidate the correct query keys on success

Source verification:
- [ ] API client barrel export includes all functions
- [ ] Hooks file imports from the API client (not raw fetch)

**Dependencies**: [Routes task]
**Blocks**: [UI page tasks]
```

### EXAMPLE — UI Component Task (Pure / Presentational)

```
### Task N: [ComponentName] component
**Priority**: N
**Spec**: `specs/NNN-ui.md`
**Complexity**: S
**Acceptance criteria**: [Description of what component renders]; [states: loading, empty, error, populated]

**Files to create**:
- `[path/to/components/domain/ComponentName.tsx]`

**Required Tests**:
Unit (component render):
- [ ] Renders [primary content] from props
- [ ] Renders [secondary content] when [condition]
- [ ] Does not render [section] when [data] is empty/null
- [ ] Shows skeleton/loading state when `isLoading` prop is true
- [ ] Shows empty state message when [data] is empty array
- [ ] Action buttons ([list]) are present and have accessible names
- [ ] [Conditional rendering assertion]

**Dependencies**: None (pure UI component)
**Blocks**: [Page tasks that compose this component]
```

### EXAMPLE — UI Page Task (Full Feature)

```
### Task N: [PageName] page
**Priority**: N
**Spec**: `specs/NNN-ui.md`
**Complexity**: M
**Acceptance criteria**: [Layout description]; [full user flow]; [navigation behavior]

**Files to create**:
- `[path/to/pages/PageName.tsx]`

**Files to modify**:
- `[path/to/App.tsx or router]` -- add route: `<Route path="[path]" element={<PageName />} />`

**Required Tests**:
Unit (component render):
- [ ] Renders [primary section] in the [position] panel
- [ ] Before [action]: [initial state description]
- [ ] After [action]: renders [result component]
- [ ] [Interactive behavior assertion]
- [ ] Loading state shows skeleton/spinner
- [ ] Error state shows clear error message

Integration:
- [ ] [Mutation] sends correct payload to [API endpoint]
- [ ] After successful [action], navigates to [destination]
- [ ] Error state shows clear error message if [operation] fails

Source verification:
- [ ] Route registered in [router file]
- [ ] Page imports [list of child components]

**Dependencies**: [API client tasks, component tasks]
**Blocks**: None (or downstream pages)
```

---

<!-- Real tasks go below. The Plan Architect deletes everything above and writes actual tasks. -->
